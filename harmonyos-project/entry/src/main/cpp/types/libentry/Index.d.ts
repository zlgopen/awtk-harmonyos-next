export const init: (resmgr: resourceManager.ResourceManager, filesDir: string, tempDir: string, screenDensity: number,
  fontScale: number, language: string) => number;

export const update: () => number;
